#include "cpp/renderer.h"


int main() {
    clyde::Renderer render(32, 32, clyde::FPS_15);
    clyde::object_list objects = {{1, 0, 'A'}};
    std::chrono::milliseconds timespan(render.fps);














    return 0;
}
